apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: empatia
  labels:
    app: gateway
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: gateway
