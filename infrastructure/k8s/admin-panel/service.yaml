apiVersion: v1
kind: Service
metadata:
  name: admin-panel
  namespace: empatia
  labels:
    app: admin-panel
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: admin-panel
