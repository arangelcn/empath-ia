apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: empatia
  labels:
    app: ai-service
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
      name: http
  selector:
    app: ai-service
