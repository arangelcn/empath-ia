apiVersion: v1
kind: Service
metadata:
  name: emotion-service
  namespace: empatia
  labels:
    app: emotion-service
spec:
  type: ClusterIP
  ports:
    - port: 8003
      targetPort: 8003
      name: http
  selector:
    app: emotion-service
